VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ARIA AI runs server-side via /api/ai. Set this in the Vercel project
# (NOT VITE_-prefixed — it must never reach the browser). The app works
# without it: AI degrades gracefully to a friendly "demo mode" message.
# ANTHROPIC_API_KEY=sk-ant-...
